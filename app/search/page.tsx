// app/search/[id]/page.tsx

import { redirect } from 'next/navigation'

// Эта страница была предназначена для просмотра старых чатов из истории.
// Поскольку функция истории теперь полностью отключена, мы не должны
// пытаться загрузить данные из Redis.
// Вместо этого мы просто немедленно перенаправляем пользователя на главную страницу.
// Это полностью предотвращает ошибку.
export default function ChatHistoryPage() {
  redirect('/')
}
